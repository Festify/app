language: node_js

node_js:
  - 'node'

cache:
  yarn: true

addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    packages:
    - gcc-4.8
    - g++-4.8

before_script:
  - bower install

script:
  - gulp build
